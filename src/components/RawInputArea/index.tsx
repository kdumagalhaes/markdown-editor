export function RawInputArea() {
    return (
        <div>
        <h1 className="title">Enter Markdown</h1>
        <textarea className="raw-area" name="raw-area" id="" cols={30} rows={10}></textarea>
        </div>
    )
}